# 废弃

这里的东西已经被取代，留着只是为了万一要回查，日常不用看。

| 文件 | 状态 |
|---|---|
| `fetch_wechat_articles.py` | 被 `脚本/fetch_all.py` 取代（后者支持 xlsx / txt 两种来源、断点续跑、失败重试） |
| `README_fly_articles.md` | 旧版说明，描述的 `wechat_articles/` 目录已不存在 |

确认用不上就可以整个删掉。
