# 指南 2：每日交易要点 SOP

> 用途：定义每天的固定流程——你上传最新文章链接和群聊截图，说一句「告诉我今天该关注什么」，我按本文件产出结合你持仓的当日要点。
> 配套文件：`指南1-交易风格与策略体系.md`（体系与方法论）、`群聊/如何总结群聊截图.md`（截图提炼规范）

---

## 一、每日流程总览

```
你做：
  1. 把大号 / 小号最新文章链接贴进 数据源/ 下的 txt（或直接发给我）
  2. 把群里的重要截图丢进 群聊/待处理/
  3. 说「告诉我今天该关注什么」

我做：
  1. 抓取新文章 → docs/ → 清洗 → docs_clean/
  2. 扫 群聊/待处理/ → 按规范提炼 → 群聊/总结/<日期>.md → 原图归档
  3. 读滚动窗口：今天 / 近 3 天 / 近 1 周（必要时回溯到 3 周）
  4. 跑 脚本/ma.py 取持仓股均线和偏离度
  5. 按第五节格式产出 每日要点/<日期>.md
```

**必须在 15:00 收盘后跑行情。** 盘中取到的是实时价，同一天 11:00 和 11:24 能差 1%，
均线和偏离度会跟着错。盘中先出的版本要在文件顶部标「盘中快照」，收盘后重跑覆盖。

---

## 二、目录约定

| 东西 | 位置 |
|---|---|
| 文章链接 txt | `数据源/苍蝇的二级市场-<月>月.txt`、`数据源/苍蝇的五级市场-<月>月.txt` |
| 文章原文 → 清洗后 | `docs/<账号>/<年>/<月>/` → `docs_clean/<账号>/<年>/<月>/` |
| 群聊截图 | 上传到 `群聊/待处理/` → 归档到 `群聊/已归档/<日期>/` |
| 群聊结论 | `群聊/总结/<YYYY-MM-DD>.md`（一小段话，直接可用） |
| 持仓 | `数据源/持仓.json`（股数/成本手工更新），截图存 `数据源/持仓截图/` |
| 每日产出 | `每日要点/<YYYY-MM-DD>.md` |
| 脚本 | `脚本/`（`ma.py` `fetch_all.py` `clean_docs.py`） |

**`数据源/` 不进 git**（账户数据），只在本地。仓库里留了 `持仓.example.json` 作为格式模板。
**群聊只有原图不进 git**（`待处理/`、`已归档/` 有群友头像和昵称），
提炼后的 `总结/` 和 `如何总结群聊截图.md` 进仓库——里面博主一律写「他」，不出现任何昵称。

**链接格式**：每行 `月.日 链接`，例如 `8.25 https://mp.weixin.qq.com/s/xxxx`

```bash
python3 脚本/fetch_all.py --from-txt 数据源/苍蝇的二级市场-8月.txt --account 苍蝇的二级市场
python3 脚本/fetch_all.py --from-txt 数据源/苍蝇的五级市场-8月.txt --account 苍蝇的五级市场
python3 脚本/clean_docs.py
```
已下载的自动跳过，重复跑无害。从微信复制的链接若带 `?poc_token=...`，脚本会自动剥离（令牌会过期，留着反而抓不到）。

---

## 三、群聊截图

提炼规范单独写在 **`群聊/如何总结群聊截图.md`**，处理截图时按那份执行。

要点：每次出要点前先看 `群聊/待处理/` 有没有图，有就处理，没有就跳过，不用你额外说明。
产出是 `群聊/总结/<日期>.md` 里的一小段话——**出每日要点时直接读这段，不再回看原图**。

**权重**：群聊里博主本人的发言等同小号权重（是他不便公开写的即时判断），
群友发言只作线索，未核实的消息不当事实用。和大号文章冲突时以文章为准。

---

## 四、滚动窗口与信息权重

| 窗口 | 作用 |
|---|---|
| 今天 | 当日操作依据 |
| 近 3 天 | 判断他的态度有没有变、规则有没有收紧 |
| 近 1 周 | 主线判断、板块态度 |
| 近 3 周 | 只在出现明显转向、或你问「最近整体怎么看」时回溯 |

**两个号的合并原则**（见指南 1 第一节）：
- 小号讲**方法论和真实态度**，大号讲**个股执行**
- 同一天两个号谈同一件事，**以小号为准**——大号是能公开发的版本，小号是不方便公开的版本
- 大号「个股」段（两条分隔线之间）是逐票操作指引，信息密度最高，重点读

---

## 五、输出格式

**只要结果，不要过程。** 不列他的原话、不标【引】【算】【推】、不做态度表和规则变化表，
不写「我认为可能或许」。分析在我这边做完，给你的是结论和数字。

**用直接陈述句，不写「他说」「他的止盈锚」「他从没提过」。** 提炼完的判断就是判断，
写「偏离 34 天线 6.2%，到止盈位」，不写「正好踩在他的止盈锚上」；
写「不在体系内」，不写「他从来没提过这只票」。归属谁说的是过程，你要的是结论。
（唯一例外：某条判断刚刚发生反转，要说清楚「昨天写的 X 撤回」，让你知道是我改口了。）

**操作思路分两块：延续 / 今日新增。** 都用编号，一条一个意思。
「延续」是近一周已经确立、今天没变的框架，让你不用回翻昨天；
「今日新增」是今天两篇文章带来的变化，这是真正要看的部分。

```markdown
# YYYY-MM-DD 交易要点

## 操作思路

### 延续（近一周，未变）

1. **主战场是谁**——只做哪一两只，为什么不做其他的。
2. **现在是加仓 T 区还是减仓 T 区**——用偏离度数字说话，止盈位是偏离买入均线约 6%。
3. **不碰的**——均线全破的、不在体系内的，写清是「不加也不割」还是「完全不动」。

### 今日新增

1. （今天最重要的那个变化，通常是某只票越过了某条线或某个偏离度阈值，带动作）
2. （买卖点结构的变化：多了一档支撑、某条线失效）
3. （基本面 / 消息面的新东西，要给数字。说清是「今日操作」还是「持有理由」）
4. （判断反转：昨天写的什么撤回，为什么）

（三到五条。没有新增就写「无实质变化，按上面执行」，不要凑数。）

## 今天盯什么

1. （三到五条，编号。每条是一个可观察的条件，不是感想）

## 买卖点

| 股票 | 现价 |  | 价格 | 是哪根线 / 说明 |
|---|---|---|---|---|
| **主战场的票加粗** | 36.70 | **卖** | **37.80** | 20 日高。已偏离 34 线 +6.2%，到止盈位 |
|  |  | 买① | 36.50 | 10 日线，就在脚下 |
|  |  | 买② | 36.17 | 21 日线 |
|  |  | 买③ | **35.34** | 34 日线，最好的位置 |
| 不动的票 | 13.34 | — | — | 不动 |

（**一个价位一行**，不要把三档买点挤在一格里用「／」分隔——一换行就没法看。
股票名和现价只在该股第一行写，后面留空，视觉上自然成块。
每行只有一个数字，第三列标「卖 / 买① / 买② / 买③」，按从近到远排。
今天就在脚下的、以及最好的那一档加粗。不该动的票三列全写「—」，说明写「不动」。）

## 今天已经做的

（可用股数少于持有股数说明今天买过，T+1 未解冻。**只写比例，不写绝对股数**——
「动力今天加过仓，买入量约占该股持仓的 5%」，不写具体股数和持有量。
没有就整节删掉。）
```

**顺序是固定的**：操作思路 → 今天盯什么 → 买卖点 → 今天已经做的。
「盯什么」在买卖点前面——先知道今天等什么条件，再去看具体价位，
表格是执行时才低头查的东西，不是先读的东西。

**数据来源不写在正文。** 走哪个源、push2his 通没通，是我的事，不占你的版面。
**只有下面三种情况**才在标题下写一行，因为它们会改变表里数字的可信度：

- 盘中生成的 → 顶部标「**盘中快照**（HH:MM）」，说明均线用的是哪天的收盘价，收盘后重跑覆盖
- 有票取数失败 → 写明是哪只、哪些数字缺
- `持仓.json` 快照早于今天 → 写明是哪天的快照，比例有偏差

**取舍原则**：

- 每一句都要能变成一个动作或一个不动作。变不成的删掉。
- **仓位一律写比例，不写绝对股数和金额。** 百分比可以，绝对股数和金额不行——
  `每日要点/` 进公开仓库，比例够用来做判断，绝对值只泄露账户规模。
  **杠杆率和维保比也是绝对数字的变形，不能写**（可以本地看 ma.py 输出和持仓截图算）。
  **本文件里举例也不要用真实数量**，反例同样是泄露。
- 数字必须是算出来的真实值，宁可标「取数失败」也不估。
- 他没说过的票，不要套用他对亲儿子的规则，直接写「不在体系里」。
- 不确定的东西不写进要点，写进「明天盯什么」当观察项。

---

## 六、行情脚本

`脚本/ma.py`——东方财富 push2his 日线前复权，纯标准库（系统 python 是 externally-managed
装不了 pandas，所以没沿用 `quant_origin/em_api.py`）。

```bash
python3 脚本/ma.py                 # 读 数据源/持仓.json 里的全部持仓
python3 脚本/ma.py 600893 302132   # 指定代码
python3 脚本/ma.py --intraday      # 盘中跑：丢掉当天那根未完成日线，均线只用收盘价算
```

输出：收盘价、MA5 / MA10 / MA21 / MA34 及各自偏离度、20 日高低、破 20 日新低标记。

**取数的坑**：

- push2his 会**按 TLS 指纹过滤 urllib**（任何 header 都是 RemoteDisconnected），所以走 curl 子进程。
- 单主机连续调用会被限流（返回空 body）。脚本在 8 个编号镜像之间轮换重试，仍可能个别失败，
  隔几分钟重跑那一个即可。**不要用盘中价冒充收盘价填进表里。**
- 有时**全部 8 个镜像同时返回空 body**（curl 52），换 header 换 host 都没用。脚本会自动回落到
  sina 的日线接口（同为前复权，在 600893 上逐项核对过，均线和 20 日区间完全一致），
  输出里标 `[sina]`。

尚未实现、需要时再加：成交量分析（判断「随便给点量就能涨」）、分钟级数据。
`quant_origin/signals.py`（KDJ/MACD）用不上——他的体系只用均线。

---

## 七、边界

这份 SOP 产出的是**规则计算结果 + 需要你关注的清单**，决策权在你。

1. **他的文章是同步或滞后的，不是前瞻的。** 8-25 那篇写「21天线、34天线买的动力，昨天早盘就是很好的做T机会」，
   描述的是已经发生的事。拿当天文章生成「明天买卖指令」，等于把滞后描述当领先信号。
2. **他本人反对照抄**（见指南 1 第六节）。
3. **账户有融资杠杆，他的体系里没有这个变量。** 「跌破 21 天线也是加仓点」「跌过的股绝不割肉」在杠杆账户里含义完全不同。
   每日从 `数据源/持仓.json` 重算杠杆率和维保比：**杠杆率低时这两条规则基本可原样执行**，杠杆检查是常规核对而非硬约束；
   杠杆率抬上去之后结论就失效，必须重新判断。具体数字不写进本文件，随负债变化。

**杠杆每天照算，但不写进要点。** 只用来校验「跌破均线继续加仓」这类规则在当前杠杆下还成不成立；
真到了会影响操作的程度，在对话里口头说。

---

## 八、日常使用示例

> **你**：（把 8.26 的链接贴进 txt，把 4 张群聊截图丢进 `群聊/待处理/`）
> 　　　告诉我今天该关注什么
>
> **我**：
> 1. 跑抓取 + 清洗，拿到大号《每个东西都有自己的节奏》、小号《遵守简单的原则》
> 2. 发现 4 张新截图 → 提炼进 `群聊/总结/2026-08-26.md` → 原图移进 `群聊/已归档/2026-08-26/`
> 3. 读今天 + 近 3 天 + 近 1 周
> 4. 收盘后跑 `脚本/ma.py` 取均线和偏离度
> 5. 产出 `每日要点/2026-08-26.md`，并在对话里给结论

历史要点归档在 `每日要点/`，方便回看他的判断和实际走势的对照。
